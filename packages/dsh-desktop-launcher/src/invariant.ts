/** Invariant companion plugin (no assertions — nothing to check at runtime). */

/** Provides no assertions: the desktop-launcher owns no cross-package runtime invariants. */
export function apply(): void {}
