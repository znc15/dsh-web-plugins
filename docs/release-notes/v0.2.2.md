This release contains 12 new features, 21 bug fixes, 14 other changes.
本次发布包含 12 项新功能、21 项修复、14 项其他改动。

### New Features / 新功能

- [plugin-manager] strip duplicate-mount bundles entries after CLI mutations / CLI 变更后清理重复挂载的 bundle 条目
- [skin-center] add sound toggle and volume control for video wallpapers ([#580](https://github.com/zhu1090093659/dsh-web-ui/issues/580)) / 为视频壁纸新增声音开关与音量控制
- [community-plugins] in-GUI install and uninstall via pluginManager service / 通过 pluginManager 服务在 GUI 内安装与卸载插件
- [plugin-manager] expose dual-channel face as pluginManager cordis service / 以 pluginManager cordis 服务暴露双通道接口
- [web-ui-all] aggregate dsh-plugin-manager into the family bundle / 将 dsh-plugin-manager 聚合进全家桶
- [pet] add refined whale-girl registry variant / 新增打磨后的 whale-girl 注册变体
- [community-plugins] register dsh-notifier in the community index (event notifications) / 在社区索引登记 dsh-notifier（事件通知）
- [release] use committed bilingual notes in the release pipeline / 发布管线采用已提交的双语发布说明
- [plugin-manager] conflict repair handoff, duplicate-id auto-disable, and boot preflight / 冲突修复交接、重复 id 自动禁用与启动预检
- [plugin-manager] add CLI gateway so the tab works on the npm web runtime / 新增 CLI 网关，让插件管理页在 npm web 运行时可用
- [plugin-manager] add family plugin manager tab over the official installer channels / 在官方安装通道之上新增插件管理页
- [describe-image] add model fetch and connectivity probe to settings card / 设置卡新增模型获取与连通性探测

### Bug Fixes / 修复

- [community-plugins] drop stale npm field from Chat Summary entry / 移除 Chat Summary 条目中过期的 npm 字段
- [community-plugins] fold pnpm github/gitlab/bitbucket shorthands in git matching / git 匹配中并入 pnpm 的 github/gitlab/bitbucket 简写
- [skins] soften whale-mom bubble opacity behind one alpha variable ([#486](https://github.com/zhu1090093659/dsh-web-ui/issues/486)) / 用单个 alpha 变量柔化 whale-mom 气泡透明度
- [community-plugins] match npm installs by package identity, not range spec / 按包身份而非范围描述匹配 npm 安装
- [web-ui-all] restore pruned lockfile entries with minimal importer additions / 以最小 importer 增量恢复被修剪的锁文件条目
- [remote-web-ui] confirm before running the sidebar update ([#507](https://github.com/zhu1090093659/dsh-web-ui/issues/507)) / 执行侧边栏更新前先确认
- [skin-center] resolve real scene containers and decode loose scene projects ([#521](https://github.com/zhu1090093659/dsh-web-ui/issues/521)) / 解析真实场景容器并解码松散场景工程
- [ssh] guard HostsTab load setState against post-unmount settle / 防护 HostsTab 加载 setState 在卸载后结算
- [skin-center] neutralize the opaque app root while a wallpaper is mounted ([#505](https://github.com/zhu1090093659/dsh-web-ui/issues/505)) / 壁纸挂载期间中和不透明应用根节点
- [ssh] make the web terminal font configurable ([#577](https://github.com/zhu1090093659/dsh-web-ui/issues/577)) / 让 Web 终端字体可配置
- [liangshen] allow goal auto-rounds through the phase-1 filter ([#578](https://github.com/zhu1090093659/dsh-web-ui/issues/578)) / 允许 goal 自动轮次通过阶段一过滤器
- [skin-center] normalize comment-only patch after migration / 迁移后将仅注释补丁规范化
- [plugin-manager] owner-aware rollback, verified mutations, spec fencing ([#546](https://github.com/zhu1090093659/dsh-web-ui/issues/546)) / 归属感知回滚、已验证变更与规格围栏
- [plugin-manager] repair Windows CLI spawn, entry-id enablement, and channel-probe noise ([#546](https://github.com/zhu1090093659/dsh-web-ui/issues/546)) / 修复 Windows CLI 启动、条目 id 启用与通道探测噪音
- [sync-shared] count the plugin-manager host copies in the copy gate / 复制门禁计入 plugin-manager 宿主副本
- [plugin-manager] add the yaml package and snapshot lock entries / 新增 yaml 包与快照锁条目
- [plugin-manager] rebuild the lock as upstream verbatim plus the new importer / 按上游原样重建锁文件并加上新 importer
- [task-board] give the legacy minute-scan test an explicit timeout budget / 为遗留分钟扫描测试设定显式超时预算
- [plugin-manager] keep the unpublished package out of the aggregate bundle / 将未发布包排除在聚合 bundle 之外
- [describe-image] keep the probe timeout armed without a caller signal / 无调用方信号时保持探测超时待命
- [describe-image] fence the model probe routes with the loopback trust check / 用回环信任检查围栏模型探测路由

### Other Changes / 其他改动

- [community-plugins] remove entry-type duplicate indexes (plugin-hub, dream-skin) / 移除条目类型重复索引（plugin-hub、dream-skin）
- [skin-center] rebuild lib artifacts after dead-code prune / 死代码清理后重建 lib 产物
- [ci] drop --with-deps from the Playwright install and cache the browsers / Playwright 安装去掉 --with-deps 并缓存浏览器
- [readme] show Wallpaper Engine wallpaper import in skins section / 在皮肤章节展示 Wallpaper Engine 壁纸导入
- [readme] sync contributor list from GitHub API [skip ci] / 从 GitHub API 同步贡献者列表 [skip ci]
- [skins] remove leftover qq2006 screenshots / 移除残留的 qq2006 截图
- [readme] sync contributor list from GitHub API [skip ci] / 从 GitHub API 同步贡献者列表 [skip ci]
- [readme] sync contributor list from GitHub API [skip ci] / 从 GitHub API 同步贡献者列表 [skip ci]
- [packages] drop unused lightningcss and sidebar devDependencies / 移除未使用的 lightningcss 与 sidebar 开发依赖
- [task-board] remove legacy mount script / 移除遗留挂载脚本
- [web-ui] prune dead exports, helpers, and unused imports / 清理死导出、辅助函数与未用导入
- [release-notes] add bilingual v0.2.1 notes and fix the convention / 新增 v0.2.1 双语说明并修正约定
- [skill] note local npm login state is not fixed / 注明本机 npm 登录态不固定
- [task-board] replace the minute-scan parity oracles with a day-granular reference ([#543](https://github.com/zhu1090093659/dsh-web-ui/issues/543)) / 用日粒度参照替换分钟扫描一致性预言

---

Generated automatically by the release pipeline (scripts/release-notes.mjs). / 由发布管线自动生成（scripts/release-notes.mjs）。
